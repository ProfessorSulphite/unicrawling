# Education Counselor RAG Pipeline -- pinned to the versions verified in this
# environment (Python 3.13.14, conda env `ise-env`).

# Phase 1: crawling, parsing, semantic scoring
crawl4ai==0.9.2
playwright==1.61.0
beautifulsoup4==4.15.0
requests==2.34.2
sentence-transformers==5.6.1
torch==2.13.0
transformers==5.14.1

# Phases 2-3: NotebookLM ingestion, schema validation, web enrichment
notebooklm-py==0.7.3
pydantic==2.13.4
exa-py==2.16.0
tenacity==9.1.4

# Exporters & CLI analytics
rich>=13.0.0

# Tests
pytest==9.0.1
pytest-asyncio==1.3.0

# After installing, fetch the browser used by Crawl4AI:
#     python -m playwright install chromium
